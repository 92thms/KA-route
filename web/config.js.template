window.CONFIG = {
  SEARCH_RADIUS_KM: ${SEARCH_RADIUS_KM},
  STEP_KM: ${STEP_KM},
  MAINTENANCE_MODE: ${MAINTENANCE_MODE},
  MAINTENANCE_KEY: "${MAINTENANCE_KEY}",
  USE_ORS_REVERSE: ${USE_ORS_REVERSE}
};
